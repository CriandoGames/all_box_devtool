/// Anchor package for the `all_box` DevTools extension.
///
/// This package does not expose a runtime API. It exists so applications can
/// add it as a dev_dependency and let Flutter/Dart DevTools discover the
/// extension under `extension/devtools/`.
///
/// **PT-BR:** Pacote-âncora da extensão do DevTools para o `all_box`.
///
/// Este pacote não expõe uma API em tempo de execução. Ele existe para que
/// aplicações possam adicioná-lo como `dev_dependency` e permitir que o
/// Flutter/Dart DevTools descubra a extensão em `extension/devtools/`.
library;
